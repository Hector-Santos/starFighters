import axios from 'axios'

export function getStars(req, res, next, schema) {
    const fristUser = req.body.fristUser;
    const secondUser = req.body.secondUser
     let promise = axios.get("")
    
        
  }